/**
 * @omahi/core — pure cycle-phase logic. No browser APIs allowed in this package.
 *
 * Placeholder export until Chunk 1 (phase engine) lands; it exists so the
 * Vitest wiring has something real to import.
 */
export const CORE_NAME = 'omahi-core';
